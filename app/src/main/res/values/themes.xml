<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the whole UI; this theme only controls the window before Compose
         attaches. It avoids pulling in AppCompat / Material Components libraries. -->
    <style name="Theme.CheckpointTimer" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
